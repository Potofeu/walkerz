
  <div class="py-3"data-search-hikes-target="list">
    <h1>Liste des circuits</h1>
    <div class="row">
      <div class="col-12">
          <% @hikes.each do |hike| %>
            <div class="py-2">
              <strong><%= hike.name %></strong>
              <%= cl_image_tag hike.locations.first.photo.key, height: 150, width: 200, crop: :fill %>
              <br>Description: <%= hike.description %>
              <br>Durée: <%= hike.time %>
            </div>
          <% end %>
      </div>
    </div>
  </div>
